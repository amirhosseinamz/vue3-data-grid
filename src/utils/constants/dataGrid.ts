import {IGridConfig} from "../interfaces/dataGrid.ts";

export const INITIAL_GRID_CONFIG: IGridConfig = {
    hasPagination: false,
    hasDownloadButton: false,
    showHeader: true,
    showRowNumber: true,
    direction: 'ltr',
    textAlign: 'center'
}